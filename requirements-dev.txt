# Development and testing dependencies
# Install with: pip install -r requirements-dev.txt
# Note: polib is in requirements.txt (not here) because the
# `translate_strings` management command lives in production code and
# must be runnable in the dev/staging container.
-r requirements.txt

pytest>=9.0,<10.0
pytest-django>=4.11,<5.0
