# Python
__pycache__/
.pytest_cache/
*.py[cod]
*.egg-info/
dist/
build/
*.egg

# Django
staticfiles/
db.sqlite3
audit.sqlite3

# .mo files ARE committed — pre-compiled locally, no gettext in Docker.
# DO NOT add *.mo here. See MEMORY.md "Translation Decision".

# Virtual environment
venv/
.venv/

# Environment
.env
*.env.local
.env.staging
konote-credentials-*

# IDE
.vscode/
.idea/
.obsidian/
*.swp
*.code-workspace

# Jules (Google AI agent)
.Jules/

# Claude Code — track shared skills/commands, ignore personal config
.claude/*
!.claude/commands/
!.claude/skills/
.claude-active

# Git worktrees
.worktrees/

# OS
.DS_Store
Thumbs.db
nul
_ul*

# QA pipeline transient files (improvement tickets and pipeline log ARE tracked)
qa/.qa-handoff*.json
tests/*.log

# Code review results (stored in private konote-qa-scenarios repo)
tasks/reviews/
tasks/session-reviews/
docs/reviews/
tasks/ux-review-latest.md

# Original document files (converted to .md — originals kept locally for reference)
tasks/originals/

# Deploy scripts (kept locally, not tracked)
scripts/push-acr-local.ps1

# Docker volumes
pgdata/
audit_pgdata/

# Backup files
backups/
*.dump

# Generated documents (built from .md source)
*.docx

# Local UX feedback (screenshots + notes kept locally, not tracked)
Pasted image*.png
UX-feedback.md
